import 'package:flutter/material.dart';

class BillListingPage extends StatelessWidget {
  const BillListingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Bills'),
      ),
    );
  }
}
